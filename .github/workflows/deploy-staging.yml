name: Deploy - 测试环境

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      version:
        description: "部署版本 (留空使用 latest)"
        required: false
        default: ""

permissions:
  contents: read
  actions: read

jobs:
  deploy:
    name: 部署到测试环境
    runs-on: ubuntu-latest
    environment: BuJia
    # 防止 dependabot 或其他自动化触发时无法访问 secrets
    if: github.actor != 'dependabot[bot]'

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: 缓存 Go 模块
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      # 尝试从最近的 CI workflow 下载 Swagger 文档，失败则本地生成
      - name: 下载 Swagger 文档
        uses: dawidd6/action-download-artifact@v11
        continue-on-error: true
        with:
          workflow: ci.yml
          name: swagger-docs
          path: api/swagger/
          if_no_artifact_found: ignore

      - name: 生成 Swagger 文档（如果未下载）
        run: |
          if [ ! -f "api/swagger/swagger.json" ]; then
            echo "未找到 Swagger artifact，本地生成..."
            go install github.com/swaggo/swag/cmd/swag@latest
            swag init -g cmd/server/main.go -o api/swagger --parseDependency --parseInternal
            echo "✅ Swagger 文档已生成"
          else
            echo "✅ 使用下载的 Swagger 文档"
          fi

      - name: 构建 Linux 二进制
        run: |
          VERSION="${{ github.event.inputs.version }}"
          if [ -z "$VERSION" ]; then
            VERSION="${{ github.sha }}"
          fi

          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
          go build -ldflags="-w -s -X main.Version=${VERSION}" \
          -o bin/iot-server-linux ./cmd/server

          echo "✅ 构建完成: bin/iot-server-linux (version: ${VERSION})"

      - name: 预检与准备 SSH
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
          SSH_PORT: ${{ secrets.STAGING_PORT || '22' }}
        run: |
          set -Eeuo pipefail

          # 1) 校验必需变量
          if [[ -z "${SSH_HOST:-}" || -z "${SSH_USER:-}" || -z "${SSH_KEY:-}" ]]; then
            echo "❌ 缺少必需的 SSH 配置，请在环境 'BuJia' 设置 SSH_HOST / SSH_USER / SSH_KEY。"
            echo "当前值:"
            echo "  SSH_HOST: ${SSH_HOST:-<未设置>}"
            echo "  SSH_USER: ${SSH_USER:-<未设置>}"
            echo "  SSH_KEY:  $(if [ -z "${SSH_KEY:-}" ]; then echo "<未设置>"; else echo "<已设置>"; fi)"
            exit 1
          fi

          # 2) 准备密钥（兼容明文或 base64）
          mkdir -p ~/.ssh
          if [[ "$SSH_KEY" == *"BEGIN"* ]]; then
            printf "%s\n" "$SSH_KEY" > ~/.ssh/deploy_key
          else
            echo "$SSH_KEY" | base64 -d > ~/.ssh/deploy_key 2>/dev/null || {
              echo "❌ SSH_KEY 既不是明文私钥也不是有效的 base64 编码，请检查 secret 内容。"
              exit 1
            }
          fi
          chmod 600 ~/.ssh/deploy_key

          # 3) known_hosts 与连通性检查
          echo "🔍 添加主机到 known_hosts: $SSH_HOST:$SSH_PORT"
          ssh-keyscan -p "$SSH_PORT" -H "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

          echo "🔍 测试连通性: $SSH_HOST:$SSH_PORT"
          if ! timeout 10 bash -c "cat < /dev/null > /dev/tcp/$SSH_HOST/$SSH_PORT" 2>/dev/null; then
            echo "❌ 无法连接到 $SSH_HOST:$SSH_PORT，请检查:"
            echo "  1. 主机名/IP 是否正确"
            echo "  2. 端口是否开放"
            echo "  3. 安全组/防火墙是否允许 GitHub Actions IP 访问"
            exit 1
          fi
          echo "✅ 连通性检查通过"

      - name: 传输与部署到测试服务器
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PORT: ${{ secrets.STAGING_PORT || '22' }}
        run: |
          set -Eeuo pipefail

          echo "📦 上传二进制文件到 $SSH_HOST..."
          scp -P "$SSH_PORT" -i ~/.ssh/deploy_key \
            -o StrictHostKeyChecking=yes -o UserKnownHostsFile=~/.ssh/known_hosts \
            -o IdentitiesOnly=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=3 \
            bin/iot-server-linux "${SSH_USER}@${SSH_HOST}:/tmp/iot-server-new"

          echo "🚀 执行远程部署..."
          ssh -p "$SSH_PORT" -i ~/.ssh/deploy_key \
            -o StrictHostKeyChecking=yes -o UserKnownHostsFile=~/.ssh/known_hosts \
            -o IdentitiesOnly=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=3 \
            "${SSH_USER}@${SSH_HOST}" << 'ENDSSH'
            set -e

            echo "🚀 开始部署测试环境..."

            # 执行数据库迁移
            echo "📊 执行数据库迁移..."
            cd /opt/iot-server
            if [ -f "./bin/migrate.sh" ]; then
              ./bin/migrate.sh || echo "⚠️ 迁移脚本未找到或执行失败，跳过"
            else
              echo "⚠️ 迁移脚本未找到，跳过数据库迁移"
            fi

            # 备份当前版本
            if [ -f /opt/iot-server/iot-server ]; then
              cp /opt/iot-server/iot-server /opt/iot-server/iot-server.backup
              echo "✅ 备份完成"
            fi

            # 替换二进制
            mv /tmp/iot-server-new /opt/iot-server/iot-server
            chmod +x /opt/iot-server/iot-server
            echo "✅ 二进制文件已更新"

            # 重启服务（统一使用 docker compose）
            cd /opt/iot-server
            if command -v docker compose &> /dev/null; then
              docker compose restart iot-server
            else
              docker-compose restart iot-server
            fi

            echo "✅ 服务已重启"

            # 增强健康检查（10 秒初始等待 + 6 次重试）
            echo "🔍 健康检查中..."
            sleep 10

            HEALTH_CHECK_PASSED=false
            for i in {1..6}; do
              echo "尝试 $i/6..."
              if curl -f http://localhost:7055/healthz > /dev/null 2>&1; then
                echo "✅ 健康检查通过"
                HEALTH_CHECK_PASSED=true
                break
              fi
              sleep 5
            done

            if [ "$HEALTH_CHECK_PASSED" = false ]; then
              echo "❌ 健康检查失败，回滚中..."
              if [ -f /opt/iot-server/iot-server.backup ]; then
                mv /opt/iot-server/iot-server.backup /opt/iot-server/iot-server
                if command -v docker compose &> /dev/null; then
                  docker compose restart iot-server
                else
                  docker-compose restart iot-server
                fi
                echo "✅ 已回滚到之前版本"
              fi
              exit 1
            fi
          ENDSSH

          echo "🎉 部署成功！"

      - name: 清理 SSH 密钥
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
