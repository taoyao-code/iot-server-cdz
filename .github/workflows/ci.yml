name: CI - 持续集成

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  # 独立生成 Swagger 文档，避免重复
  swagger-docs:
    name: 生成 Swagger 文档
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: 缓存 Go 模块
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: 生成 Swagger 文档
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest
          swag init -g cmd/server/main.go -o api/swagger --parseDependency --parseInternal
          echo "✅ Swagger 文档已生成"

      - name: 上传 Swagger 文档
        uses: actions/upload-artifact@v4
        with:
          name: swagger-docs
          path: api/swagger/
          retention-days: 1

  # 并行化测试
  test:
    name: 测试 - ${{ matrix.package }}
    runs-on: ubuntu-latest
    needs: swagger-docs

    strategy:
      fail-fast: false
      matrix:
        package:
          - app
          - service
          - api
          - protocol
          - outbound
          - storage

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: iot_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: 缓存 Go 模块
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: 下载 Swagger 文档
        uses: actions/download-artifact@v4
        with:
          name: swagger-docs
          path: api/swagger/

      - name: 验证 Go 模块
        run: |
          go mod download
          go mod verify

      - name: 初始化数据库
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d iot_test -f ./db/migrations/full_schema.sql || true

      - name: 运行测试 - ${{ matrix.package }}
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: iot_test
          REDIS_HOST: localhost
          REDIS_PORT: 6379
        run: |
          go test -race -coverprofile=coverage-${{ matrix.package }}.out -covermode=atomic ./internal/${{ matrix.package }}/...

      - name: 上传覆盖率报告
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.package }}
          path: coverage-${{ matrix.package }}.out
          retention-days: 1

  # 质量检查（代码格式、静态分析、构建）
  quality:
    name: 质量检查
    runs-on: ubuntu-latest
    needs: swagger-docs

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: 缓存 Go 模块
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: 下载 Swagger 文档
        uses: actions/download-artifact@v4
        with:
          name: swagger-docs
          path: api/swagger/

      - name: 验证 Go 模块
        run: |
          go mod download
          go mod verify

      - name: 代码格式检查
        run: |
          if [ "$(gofmt -s -l . | wc -l)" -gt 0 ]; then
            echo "❌ 以下文件需要格式化:"
            gofmt -s -l .
            echo ""
            echo "请运行: make fmt"
            exit 1
          fi
          echo "✅ 代码格式检查通过"

      - name: Go vet 静态分析
        run: go vet ./...

      - name: 构建验证
        run: |
          make build
          echo "✅ 构建成功"

  # Lint 检查（强制通过，不再 continue-on-error）
  lint:
    name: Lint 检查
    runs-on: ubuntu-latest
    needs: swagger-docs

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: 缓存 Go 模块
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: 下载 Swagger 文档
        uses: actions/download-artifact@v4
        with:
          name: swagger-docs
          path: api/swagger/

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest
          args: --timeout=5m

  # 覆盖率汇总
  coverage:
    name: 覆盖率汇总
    runs-on: ubuntu-latest
    needs: test
    if: always()

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.4"

      - name: 下载所有覆盖率报告
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-*
          merge-multiple: true

      - name: 合并覆盖率报告
        run: |
          echo "mode: atomic" > coverage.out
          tail -q -n +2 coverage-*.out >> coverage.out || true

      - name: 生成覆盖率报告
        run: |
          go tool cover -func=coverage.out
          coverage=$(go tool cover -func=coverage.out | grep total | awk '{print $3}')
          echo "✅ 总体覆盖率: $coverage"

  # Docker 镜像构建
  build-docker:
    name: Docker 镜像构建
    runs-on: ubuntu-latest
    needs: [test, quality, lint]

    steps:
      - name: 检出代码
        uses: actions/checkout@v6

      - name: 下载 Swagger 文档
        uses: actions/download-artifact@v4
        with:
          name: swagger-docs
          path: api/swagger/

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 构建 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: |
            iot-server:${{ github.sha }}
            iot-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # CI 摘要
  summary:
    name: CI 摘要
    runs-on: ubuntu-latest
    needs: [test, quality, lint, coverage, build-docker]
    if: always()

    steps:
      - name: 检查结果
        run: |
          echo "📊 CI 检查结果:"
          echo "  - 测试: ${{ needs.test.result }}"
          echo "  - 质量检查: ${{ needs.quality.result }}"
          echo "  - Lint: ${{ needs.lint.result }}"
          echo "  - 覆盖率: ${{ needs.coverage.result }}"
          echo "  - Docker 构建: ${{ needs.build-docker.result }}"
          echo ""

          if [ "${{ needs.test.result }}" = "success" ] && \
             [ "${{ needs.quality.result }}" = "success" ] && \
             [ "${{ needs.lint.result }}" = "success" ] && \
             [ "${{ needs.build-docker.result }}" = "success" ]; then
            echo "✅ CI 检查全部通过！"
            exit 0
          else
            echo "❌ CI 检查失败"
            exit 1
          fi
